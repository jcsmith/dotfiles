- link:
    ~/.vim: vim/
    ~/.vimrc: vim/vimrc
    ~/.bash_aliases: bash/bash_aliases
    ~/.bashrc: bash/bashrc
    ~/.signature: signatures/signature
    ~/.signature-no-phone: signatures/signature-no-phone

- shell:
    - [umask 077; mkdir ~/.vimundo, Creating Directory for Vim persistent undo]

