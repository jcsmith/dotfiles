- link:
    ~/.bash_aliases: bash/bash_aliases
    ~/.bash_private: bash/bash_private
    ~/.bashrc: bash/bashrc
    ~/.git-hooks: git-hooks
    ~/.signature-no-phone: signatures/signature-no-phone
    ~/.signature: signatures/signature
    ~/.tmux.conf: tmux/tmux.conf
    ~/.vim: vim/
    ~/.vimrc: vim/vimrc

- shell:
    - [umask 077; mkdir -p ~/.vimundo, Creating Directory for Vim persistent undo]

